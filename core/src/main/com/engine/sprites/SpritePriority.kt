package com.engine.sprites

/**
 * An enum class that represents the rendering priority (order) of a [Sprite].
 *
 * @see Sprite
 */
enum class SpritePriority {
  BACKGROUND,
  PLAYGROUND,
  FOREGROUND
}
